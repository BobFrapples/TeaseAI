<UserControl x:Class="TeaseAI.PersonalityEditor.Views.ScriptListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TeaseAI.PersonalityEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="300">
    <StackPanel Style="{StaticResource ListItemStackPanelStyle}">
        <TextBlock Text="{Binding Name}" 
                   TextWrapping="Wrap" 
                   Style="{StaticResource ListItemHeaderStyle}"
                   />

        <TextBlock Text="{Binding SessionPhase}" TextWrapping="Wrap"/>
        <TextBlock Text="{Binding Info}" TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>
